import { IDefinition } from "../../src/codegen/types";

const def: IDefinition = {
    domain: {
        name: 'Sample',
        version: '1',
    },

    struct: [
    ],
    
    related: [
    ]
};

export default def;